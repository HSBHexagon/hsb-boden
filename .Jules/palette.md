## 2024-08-22 - Form Label Required Indicators in CSS Grids
**Learning:** When appending inline visual indicators (like required asterisks) to form labels using CSS grid layouts, the grid can force the text and indicator onto separate rows.
**Action:** Always wrap the label text and the indicator in a `<span>` container to keep them inline.
