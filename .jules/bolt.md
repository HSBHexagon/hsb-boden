- V8 engine optimization makes chained `.filter().map()` perform nearly identically to manual `.reduce()` or `for` loop traversal for small arrays (175 elements) containing objects, sometimes even outperforming it. Removing the double-traversal is mostly an algorithmic theoretical win and helps readability by avoiding an intermediate array, but doesn't produce measurable gains in JS engines because the allocation impact is microscopic.
