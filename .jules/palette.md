## 2024-05-15 - Inline Form Layouts with CSS Grid
**Learning:** When appending inline visual indicators (e.g., required asterisks) to form labels using CSS grid layouts, the grid forces them onto separate rows if appended directly.
**Action:** Always wrap the label text and the inline indicator together in a `<span>` container to maintain inline rendering within grid cells.
