# PR-Triage HSB/HEXAFLOOR — 2026-06-24 (read-only)

> Stand: 35 offene PRs, alle Bot-generiert (Jules + Dependabot). Erstellt Claude Code (Opus 4.8).
> **Keine PR ist autonom mergebar** — jeder Merge ändert Code/Deps/CI und ist laut `CLAUDE.md` freigabepflichtig.
> **Leitlinie:** Vor dem Production-Cutover (Phase C) den Repo-Stand **einfrieren**. Was deployt wird, soll nicht kurz vorher noch geändert werden. Merges erst **nach** verifiziertem Go-Live.

## Empfehlung in einem Satz
Jetzt nur die **4 obsoleten Jules-Konsolidierungs-Drafts schließen** (reversibel, kein Code). Alles andere bis nach Go-Live halten, dann gebündelt entscheiden.

## Gruppe 1 — Jules-Konsolidierungs-Drafts → SCHLIESSEN (obsolet)
Meta-PRs „resolve conflicts / consolidate", entstanden gegen einen alten main-Stand. Geschwister #79/#80 wurden bereits als obsolet geschlossen (Handoff 22.06. 12:06).
- **#84** „chore: consolidate open Jules PRs after main" (draft)
- **#70** „chore: resolve open postskript and perf PRs" (draft)
- **#68** „chore: cumulative resolution of open tasks and conflicts" (draft)
- **#54** „chore: cumulative resolution of open tasks and conflicts"
→ Empfehlung: schließen mit Hinweis, dass main bereits weiter ist. **Reversibel** (reopen möglich).

## Gruppe 2 — Dependabot → HALTEN / nach Go-Live
- **#82** npm-**major**-group (12 Updates) — HOHES Risiko. Überschneidet sich mit der bereits **manuell** durchgeführten Astro-6/Tailwind-4/Cloudflare-13-Migration (Commit `5030504`). Wahrscheinlich **veraltet/konfliktär** → nach Go-Live prüfen, voraussichtlich schließen oder als eigene Migration behandeln.
- **#81** npm-patch-minor-group (12 Updates) — moderat. Nach Go-Live, mit `npm run check/test:run/build` + grüner CI verifizieren.
- **#74** form-data 4.0.5→4.0.6 (dev-dep, Patch) — niedrig/sicher. Nach Go-Live mergebar.
- **#48** lighthouse-ci-action bump — CI-only. Nach Go-Live.
- **#47** actions-**major**-group (5 Updates) — CI-Actions-Major, kann Workflows brechen. Nach Go-Live, einzeln prüfen.
- **#46** trufflehog 3.82→3.95 (Security-Scanner-Action) — niedrig/sicher. Nach Go-Live.

## Gruppe 3 — Jules Perf-Opts → HALTEN (geringer Wert, Code-Risiko)
Mikro-Optimierungen an Daten-Lookups einer statischen Marketing-Site; mehrere überlappen sich.
- #78 „O(1) lookups", #67, #64, #62, #53, #52, #51 (mehrfach „optimize getPublicReferences/content lookups").
→ Empfehlung: nach Go-Live **höchstens einen** übernehmen, Rest als Duplikate schließen. Vor Cutover nichts davon.

## Gruppe 4 — Jules Test-Adds → HALTEN / dedupen
Additive Tests, viele **Duplikate** (z.B. `getReferencesForSlugs` in #60/#57, `buildFaqJsonLd` in #59/#27, `trackEvent` in #63/#37).
- #63, #61, #60, #59, #57, #56, #37, #36, #27, #25.
→ Empfehlung: nach Go-Live dedupen, je echten Neuzugang einzeln reviewen.

## Gruppe 5 — Jules Cleanup/Remove-unused → HALTEN / dedupen
„Remove unused function" — niedriges Risiko, aber Code-Änderung + Duplikate (`validateSiteContent` in #65/#17/#16; `trackEvent` in #66/#8; `getServiceBySlug` #30).
- #66, #65, #58, #30, #17, #16, #8.
→ Empfehlung: nach Go-Live einen pro Funktion übernehmen, Rest schließen.

## Gruppe 6 — CI
- **#55** „optimize ci checks, security scanners and linting" (draft) — nach Go-Live reviewen.

## Branch-Protection (separat)
Klassische Branch-Protection auf `main` = nicht gesetzt (404); Ruleset „Protect Main" aktiv, aber admin/Jules-umgehbar (erklärt frühere „Bypassed rule violations"). Nachschärfen ist sinnvoll, aber **nach** dem Cutover — vorher würde es die eigenen Cutover-/Doku-Pushes blockieren.
