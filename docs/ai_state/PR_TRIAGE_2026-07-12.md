# PR-Triage — HSBHexagon/hsb-boden

Stand: 2026-07-12 ~00:20 UTC. Methode: `gh api --paginate` (REST, 52 PRs) + unabhängige
Search-Gegenprobe (52) → kein `INVENTORY_MISMATCH`. Duplikat-Belege per vollständigem
`gh pr diff`-Hunk-Vergleich (Subagent) mit Lead-Stichproben-Gegenprüfung.

**Governance:** Merges sind nicht freigegeben (Owner-Gate). Schließungen belegter
Duplikate erst nach Codex-Adversarial-Review. Diese Matrix ist eine Empfehlungsliste.

## Entscheidungsmatrix

| Klasse | PRs | Beleg |
|---|---|---|
| SUPERSEDED → Schließ-Kandidat | #26 (≡#23), #30 (≡#22), #35 (≡#24) | byte-identische Diff-Hunks, doppelt geprüft |
| SUPERSEDED → Schließ-Kandidat | #63, #61, #57 | LeadForm.tsx-Änderungen vollständig von #54-Inhalt abgedeckt |
| OBSOLETE → Schließ-Kandidat | #49 | enthält NUR Workflow-Beifang (+4/-2), keinen Caching-Code trotz Titel |
| MERGE-Empfehlung (sauber) | #22, #23, #24, #33 (Lang-Import-Cleanups), #56 (trufflehog-Pin), #58 (Header), #62 (LCP-Kern), #38 (decoding=async) | kleine fokussierte Diffs ohne Beifang |
| REBASE_REQUIRED (Beifang entfernen) | #54 (Workflow-/Token-Änderungen + Wegwerf-Skript + PNGs; LeadForm-Anteil wertvoll → sauber extrahieren), #60 (pnpm-lock.yaml +8091 im npm-Repo; astro.config/Header-Anteile einzigartig), #55, #39, #46, #47 (Workflow-Beifang), #4/#5 (Dependency-Groups, Basis veraltet) | Dateilisten geprüft |
| HIGH_RISK → Owner-Review | #48 (React-Removal, +1296/-4376), #12/#13 (AI-CI-Workflows: Secrets/Kosten/Datenfreigabe), #15 (CI-Restore, alt) | Architektur-/Security-Tragweite |
| CLARIFY | #59 (3D site survey Content) | fachliche Freigabe unklar |

## Systemischer Befund

Fast alle Bot-PRs (Jules „Palette"/„Bolt") tragen identischen Workflow-Beifang: Sie
versuchten, den „Workers Builds"-CI-Fehler zu umgehen, dessen Root Cause der stale
Workers-Entrypoint in `wrangler.toml` war (Journal-Beleg in #60). Der Branch
`fable5/deploy-truth-consolidation` behebt die Root Cause; danach Rebase-Kandidaten
neu bewerten — der Beifang entfällt.

## Empfohlene Reihenfolge

1. `fable5/deploy-truth-consolidation` mergen (Owner) → Root Cause weg.
2. Schließungsliste (#26/#30/#35/#63/#61/#57/#49) nach Codex-Adversarial-Gate mit
   nachvollziehbarem Kommentar schließen.
3. Saubere Merge-Kandidaten einzeln prüfen/mergen (Owner), beginnend mit #56.
4. LeadForm-Extrakt aus #54 als frischer PR; #60-Kern ohne Lockfile neu.
5. #48/#12/#13 als eigene Owner-Termine (Messbeleg-Pflicht für React-Removal).
