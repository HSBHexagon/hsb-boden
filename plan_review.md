1. **Identify Performance Bottleneck**: The above-the-fold hero images in `src/pages/branchen/[slug].astro` and `src/pages/leistungen/[slug].astro` are currently using the `loading="lazy"` attribute. This is an anti-pattern that delays the Largest Contentful Paint (LCP) because the browser waits for layout calculation before initiating the image fetch.
2. **Implement LCP Optimization**: Update these critical LCP candidate images to use `fetchpriority="high" loading="eager" decoding="async"` instead. This will instruct the browser's preload scanner to fetch the assets immediately at high priority. Add comments to explain the optimization.
3. **Extend to Referenzen**: Apply the same optimization to the first large image in `src/pages/referenzen/index.astro`, which is also positioned above the fold.
4. **Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done**: Run `pnpm check` and `pnpm test:run` to ensure no errors were introduced.
5. **Document & PR**: Add an entry to `.jules/bolt.md` reflecting on the LCP anti-pattern in the dynamic routes. Finally, submit the PR with the title "⚡ Bolt: Eager load hero images to improve LCP" and the required details.
