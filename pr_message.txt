🧪 [testing improvement] Add test for buildArticleJsonLd

🎯 **What:** The testing gap addressed
The `buildArticleJsonLd` function in `src/lib/schema.ts` lacked test coverage, creating a gap in validating the generation of JSON-LD schemas for articles.

📊 **Coverage:** What scenarios are now tested
A new test block ensures that passing an article object (with headline, description, and path) correctly returns a valid JSON-LD graph representing a TechArticle with populated attributes such as the correct schema `@type`, headline, description, author, and publisher.

✨ **Result:** The improvement in test coverage
`src/lib/schema.ts` is now better tested. The test suite catches regressions for all exposed schema generation functions in `src/lib/schema.ts`.
